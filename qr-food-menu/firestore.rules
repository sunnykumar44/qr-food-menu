rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo rule set for beginner setup.
    // In production, secure writes with Firebase Authentication.
    match /shops/{shopId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
