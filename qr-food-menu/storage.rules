rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Demo rule set for beginner setup.
    // In production, secure uploads with Firebase Authentication.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
